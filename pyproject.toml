[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "astrameter"
version = "2.3.0"
description = "Power-meter monitoring for Marstek battery inverters (formerly b2500-meter)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-or-later" }
dependencies = [
    "aiohttp>=3.14.3",
    "requests",
    "urllib3>=2.6.3,<3",
    "aiomqtt>=2.0.0,<3",
    "jsonpath-ng",
    "pymodbus==3.6.5",
    "smllib",
    "pyserial-asyncio-fast",
    "configupdater>=3.2",
    "aioesphomeapi>=36.0.1",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.8.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-timeout>=2.0.0",
    "mypy>=1.13.0",
]
sim = [
    "textual>=1.0.0",
    "textual-plot>=0.10.1",
]

[project.scripts]
astrameter = "astrameter.main:main"
astra-sim = "astrameter.simulator.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/astrameter"]

[tool.pytest.ini_options]
testpaths = ["tests", "src"]
python_files = ["test_*.py", "*_test.py"]
addopts = "-ra -q"
asyncio_mode = "auto"
timeout = 90
markers = [
    "esphome_e2e: cross-backend e2e suite that also drives the compiled ESPHome host binary (run via the ct002-host-e2e CI job; esphome params skip without the CLI)",
]

[tool.ruff]
target-version = "py310"
line-length = 88
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
# Formatter handles wrapping; avoid duplicate line-length noise with E501
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["astrameter"]

[tool.mypy]
python_version = "3.10"
warn_unused_configs = true
warn_unused_ignores = true
warn_return_any = false
ignore_missing_imports = true
explicit_package_bases = true
namespace_packages = true
mypy_path = ["src"]
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_defs = true
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
# Whether these suppressions are needed depends on whether the optional `sim`
# extra is installed: with textual present they silence a real signature
# mismatch, without it the call is untyped and they read as unused. CI lints
# with `dev` only, so the module is exempt rather than wrong in one of the two.
module = "astrameter.simulator.tui"
warn_unused_ignores = false
