name: CI Pipeline

on:
  push:
    branches: [ "main", "develop" ]
    tags: [ "*.*.*" ]
  pull_request:
    branches: [ "main", "develop" ]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Ruff check
        run: uv run ruff check .

      - name: Ruff format
        run: uv run ruff format --check .

      - name: Mypy
        run: uv run mypy src/

  ct002-host-protocol:
    needs: [ lint ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install build tooling
        run: sudo apt-get update -qq && sudo apt-get install -y -qq cmake g++

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Run host-gcc protocol + wrappers + balancer parity tests
        run: uv run pytest tests/components/ct002/test_host_protocol.py -v

  # Discover the ct002 compile targets dynamically so adding a
  # tests/components/ct002/test.*.yaml file is automatically compiled in CI
  # without editing this workflow.
  ct002-esphome-yaml:
    needs: [ lint ]
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.find.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Collect test YAML files
        id: find
        run: |
          matrix=$(ls tests/components/ct002/test.*.yaml | jq -R -s -c 'split("\n")[:-1]')
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  ct002-esphome-compile:
    needs: [ ct002-esphome-yaml ]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        yaml: ${{ fromJSON(needs.ct002-esphome-yaml.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install ESPHome
        run: pip install esphome

      - name: Compile ${{ matrix.yaml }}
        run: esphome compile ${{ matrix.yaml }}

  ct002-host-e2e:
    needs: [ lint ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies (astrameter + esphome)
        run: |
          uv sync --frozen --extra dev
          uv tool install esphome

      # Build both host binaries in their own steps so a compile failure
      # shows up here (with full gcc output) rather than as an opaque pytest
      # timeout inside the module fixtures. The fixtures find the binaries
      # already built and skip recompiling.
      - name: Build ct002 host binary
        run: esphome compile tests/components/ct002/test.host.yaml

      - name: Build ct002 test-hooks (e2e control) binary
        run: esphome compile tests/components/ct002/test.e2e.host.yaml

      # All cross-backend suites are tagged `@pytest.mark.esphome_e2e`
      # (see pyproject.toml). Selecting by marker means new suites are
      # picked up automatically without editing this workflow. Their
      # `python` params also run in the `validate` job; the `esphome`
      # params only run where the esphome CLI + test-hooks binary exist.
      - name: Run host-platform E2E (cross-backend esphome suites)
        run: uv run pytest -m esphome_e2e -v

      # The codegen unit tests (test_codegen.py) `importorskip("esphome")` —
      # they import the esphome Python module to exercise the ct002 schema
      # validators directly, so they need esphome importable in the venv that
      # runs pytest (the `uv tool install` above only puts the CLI on PATH).
      # They're not `esphome_e2e`-marked, so without this they skip in every
      # job and never actually run. Install esphome into the project venv
      # last (so it can't perturb the e2e run above) and run them with
      # --no-sync so `uv run` doesn't drop esphome (it isn't in uv.lock).
      - name: Install esphome into venv for codegen tests
        run: uv pip install esphome

      - name: Run ct002 codegen unit tests (esphome schema validators)
        run: uv run --no-sync pytest tests/components/ct002/test_codegen.py -v

  # The add-on image, exercised the way the Supervisor starts it: the app
  # reads its configuration from the add-on options and the Supervisor API,
  # with no config file involved, so nothing but a real container run covers
  # run.sh, the venv path and SUPERVISOR_TOKEN reaching the app.
  addon-container:
    needs: [ lint ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      # Loaded into the local daemon (single platform) so the tests can run it.
      - name: Build the add-on image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./ha_addon/Dockerfile
          platforms: linux/amd64
          load: true
          tags: astrameter-addon:test
          build-args: |
            BUILD_FROM=ghcr.io/hassio-addons/base:17.0.1
          cache-from: type=gha,scope=addon-container
          cache-to: type=gha,mode=max,scope=addon-container

      - name: Run the add-on container tests
        run: uv run pytest tests/test_addon_container.py -v

  # The dashboard bundle is a COMMITTED generated artifact (neither the Docker
  # build nor `esphome compile` has Node), so CI has to prove it still matches
  # its source. Deliberately unfiltered and not fork-gated: `npm run check`
  # otherwise runs only in the pages/preview workflows, both filtered to
  # `web/**`, so a Python-only PR could land a stale bundle with green CI.
  web:
    needs: [ lint ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        working-directory: web
        run: npm ci

      # Typecheck, unit tests, and the dashboard drift + size-budget gate.
      - name: Check
        working-directory: web
        run: npm run check

      - name: Show dashboard bundle size
        working-directory: web
        run: |
          gz=$(gzip -9 -c ../src/astrameter/static/dashboard.html | wc -c)
          echo "dashboard.html: $(wc -c < ../src/astrameter/static/dashboard.html) B raw, ${gz} B gzipped" >> "$GITHUB_STEP_SUMMARY"

  # Browser-level tests against the real stack: the battery simulator speaking
  # CT002 UDP, a real AstraMeter, and the committed dashboard bundle. These
  # catch what the string-rendered unit tests structurally cannot — a control
  # destroyed by a re-render, a disclosure that snaps shut, a write that never
  # reaches the device.
  dashboard-e2e:
    needs: [ lint, web ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install Python dependencies
        run: uv sync --frozen --extra dev --extra sim

      - name: Install web dependencies
        working-directory: web
        run: npm ci

      - name: Install Chromium
        working-directory: web
        run: npx playwright install --with-deps chromium

      - name: Run dashboard end-to-end tests
        working-directory: web
        run: npm run e2e

      - name: Upload the report on failure
        if: ${{ failure() }}
        uses: actions/upload-artifact@v7
        with:
          name: dashboard-e2e-report
          path: |
            web/playwright-report
            web/test-results
          retention-days: 7

  validate:
    needs: [ lint, web ]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.10", "3.11", "3.12", "3.13" ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-suffix: ${{ matrix.python-version }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install mosquitto (for MQTT integration tests)
        run: sudo apt-get update -qq && sudo apt-get install -y -qq mosquitto

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Run tests
        run: uv run pytest --cov=astrameter --cov-report=xml --junitxml=test-results.xml

      - name: Upload test results
        if: ${{ always() }}
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ matrix.python-version }}
          path: test-results.xml

      - name: Upload coverage XML
        if: ${{ always() }}
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.python-version }}
          path: coverage.xml

  # Steering-quality evaluation (issue #458): runs the deterministic
  # active-control simulation suite (src/astrameter/simulator/evaluation.py)
  # on the PR base and head and posts the before/after metrics as a sticky
  # PR comment. Purely informational — thresholds that must hold live in
  # pytest regressions, not here.
  #
  # The suite is split across four jobs so every scenario runs in its own
  # matrix worker in parallel, cutting wall time from ~(N_scenarios * seeds)
  # sequential minutes down to roughly one scenario's worth.
  #
  # That parallelism costs one runner per scenario, twice over (base + head),
  # and the whole point of the run is the PR comment — so the gate below keeps
  # it off every push and off any PR whose diff cannot move the numbers.
  steering-eval-gate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.decide.outputs.run }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          # The base commit has to be present to diff against it.
          fetch-depth: 0

      - name: Decide whether the evaluation is needed
        id: decide
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          # Escape hatch for a change that moves steering from somewhere the
          # paths below don't cover (a new dependency, a refactor elsewhere in
          # the loop): label the PR `steering-eval` to force a run.
          FORCED: ${{ contains(github.event.pull_request.labels.*.name, 'steering-eval') }}
          # What the simulation actually reads: the controller under test, the
          # simulated plant and the harness itself — plus this workflow, so a
          # change to the eval jobs is exercised by the PR that makes it.
          RELEVANT: '^(src/astrameter/(ct002|simulator)/|\.github/workflows/ci\.yml$)'
        run: |
          decide() {
            echo "run=$1" >> "$GITHUB_OUTPUT"
            echo "$2"
            if [ "$1" = "false" ]; then
              echo "Steering evaluation skipped — $2" >> "$GITHUB_STEP_SUMMARY"
            fi
          }
          if [ "$FORCED" = "true" ]; then
            decide true "forced by the 'steering-eval' label."
            exit 0
          fi
          # Fail open: if the base commit is somehow unreachable, run the suite
          # rather than silently dropping the comparison.
          if ! changed=$(git diff --name-only "$BASE_SHA...HEAD"); then
            decide true "could not diff against $BASE_SHA; running anyway."
            exit 0
          fi
          echo "Changed files:"
          echo "$changed"
          # Tests next to the code can't move the metrics — the harness never
          # imports them — so a test-only edit under those paths still skips.
          changed=$(printf '%s\n' "$changed" | grep -vE '_test\.py$' || true)
          if printf '%s\n' "$changed" | grep -qE "$RELEVANT"; then
            decide true "the diff touches the active-control loop."
          else
            decide false "the diff touches nothing the simulation reads."
          fi

  steering-eval-discover:
    needs: [ lint, steering-eval-gate ]
    if: needs.steering-eval-gate.outputs.run == 'true'
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.list.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: List scenarios
        id: list
        run: |
          matrix=$(uv run python -m astrameter.simulator.evaluation --list \
            | awk '{print $1}' \
            | jq -R -s -c 'split("\n")[:-1]')
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  steering-eval-run:
    needs: [ steering-eval-discover ]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        scenario: ${{ fromJSON(needs.steering-eval-discover.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Compute safe artifact name
        id: safe
        run: |
          safe=$(echo '${{ matrix.scenario }}' | tr '/' '-')
          echo "name=steering-eval-${safe}" >> "$GITHUB_OUTPUT"
          echo "safe=${safe}" >> "$GITHUB_OUTPUT"

      - name: Run evaluation (head)
        run: |
          uv run python -m astrameter.simulator.evaluation \
            --scenario '${{ matrix.scenario }}' \
            --json "head-${{ steps.safe.outputs.safe }}.json"

      # The base run uses the base commit's own harness + controller. Until the
      # harness exists on the base branch — or for a scenario this PR adds that
      # the base doesn't have yet — this produces an empty array and the compare
      # job shows head-only numbers for this scenario.
      - name: Run evaluation (base)
        if: github.event_name == 'pull_request'
        run: |
          safe='${{ steps.safe.outputs.safe }}'
          scenario='${{ matrix.scenario }}'
          base_out="$GITHUB_WORKSPACE/base-${safe}.json"
          git worktree add /tmp/steering-base "${{ github.event.pull_request.base.sha }}"
          if [ -f /tmp/steering-base/src/astrameter/simulator/evaluation.py ]; then
            cd /tmp/steering-base
            uv sync --frozen --extra dev
            # A scenario this PR introduces won't exist on base; emit an empty
            # array (head-only compare) instead of failing the whole job.
            if uv run python -m astrameter.simulator.evaluation --list 2>/dev/null \
                 | awk '{print $1}' | grep -qxF "$scenario"; then
              uv run python -m astrameter.simulator.evaluation \
                --scenario "$scenario" --json "$base_out"
            else
              echo "[]" > "$base_out"
            fi
          else
            echo "[]" > "$base_out"
          fi

      - name: Upload scenario/seed metrics
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.safe.outputs.name }}
          path: |
            head-${{ steps.safe.outputs.safe }}.json
            base-${{ steps.safe.outputs.safe }}.json
          if-no-files-found: ignore

  steering-eval-compare:
    needs: [ steering-eval-run ]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Download all scenario metrics
        uses: actions/download-artifact@v7
        with:
          pattern: steering-eval-*
          path: eval-artifacts
          merge-multiple: true

      - name: Merge scenario JSONs
        run: |
          jq -s 'add' eval-artifacts/head-*.json > head.json
          if ls eval-artifacts/base-*.json 2>/dev/null | grep -q .; then
            jq -s 'add' eval-artifacts/base-*.json > base.json
          else
            echo "[]" > base.json
          fi

      - name: Render comparison
        if: github.event_name == 'pull_request'
        run: |
          uv run python -m astrameter.simulator.evaluation \
            --input head.json --compare base.json \
            --html steering-eval-report.html > steering-eval-comment.md
          cat steering-eval-comment.md >> "$GITHUB_STEP_SUMMARY"

      # The report is uploaded unzipped (v7 + archive:false): a single
      # self-contained file (all CSS/JS inlined, no external links) that GitHub
      # can render in the browser straight from its artifact page. archive:false
      # accepts exactly one file and uses the filename as the artifact name, so
      # the report and the raw metrics go to separate artifacts.
      - name: Upload report (unzipped, browser-viewable)
        id: report
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          archive: false
          path: steering-eval-report.html
          if-no-files-found: ignore

      - name: Upload raw metrics
        uses: actions/upload-artifact@v7
        with:
          name: steering-eval-data
          path: |
            head.json
            base.json
            steering-eval-comment.md
          if-no-files-found: ignore

      # Forks get a read-only token; they still see the table in the job
      # summary above.
      - name: Post sticky PR comment
        if: >-
          github.event_name == 'pull_request' &&
          github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/github-script@v8
        env:
          # Direct per-artifact URL from the upload step (login-required, valid
          # until the artifact/run expires) — the closest thing to a direct
          # link to the report; GitHub has no stable raw-file permalink.
          REPORT_URL: ${{ steps.report.outputs.artifact-url }}
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- steering-eval -->';
            const reportUrl = process.env.REPORT_URL;
            const link = reportUrl
              ? `\n\n📊 [**Open the interactive report**](${reportUrl}) — ` +
                `\`steering-eval-report.html\`, a single self-contained file ` +
                `(opens in-browser; download it if your browser blocks inline ` +
                `scripts).\n`
              : '';
            const body = marker + '\n' +
              fs.readFileSync('steering-eval-comment.md', 'utf8') + link;
            const {data: comments} = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100,
            });
            const existing = comments.find(
              c => c.body && c.body.startsWith(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  build:
    needs: [ validate, web ]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        config:
          - platform: linux/amd64
            cache_scope: base-linux-amd64
          - platform: linux/arm/v7
            cache_scope: base-linux-arm-v7
          - platform: linux/arm64
            cache_scope: base-linux-arm64
    uses: ./.github/workflows/build-image.yml
    with:
      registry: ghcr.io
      platform: ${{ matrix.config.platform }}
      cache_scope: ${{ matrix.config.cache_scope }}
      context: .
      dockerfile: ./Dockerfile
      image-suffix: ""
      digest-prefix: "digests-base-"
      push: ${{ github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository }}

  merge:
    needs: [ build ]
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/merge-manifests.yml
    with:
      registry: ghcr.io
      image-suffix: ""
      digest-prefix: "digests-base-"
      legacy-image-name: tomquist/b2500-meter

  build-addon:
    needs: [ validate ]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            cache_scope: addon-linux-amd64
          - platform: linux/arm64
            cache_scope: addon-linux-arm64
    uses: ./.github/workflows/build-image.yml
    with:
      registry: ghcr.io
      platform: ${{ matrix.platform }}
      cache_scope: ${{ matrix.cache_scope }}
      context: .
      dockerfile: ./ha_addon/Dockerfile
      build-args: |
        BUILD_FROM=ghcr.io/hassio-addons/base:17.0.1
      image-suffix: "-addon"
      digest-prefix: "digests-addon-"
      push: ${{ github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository }}

  merge-addon:
    needs: [ build-addon ]
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/merge-manifests.yml
    with:
      registry: ghcr.io
      image-suffix: "-addon"
      digest-prefix: "digests-addon-"
      legacy-image-name: tomquist/b2500-meter
